{
  "imports": {
    "@std/fs": "jsr:@std/fs@^1.0.20"
  },
  "tasks": {
    "run": {
      "description": "Run the main exe in the sandbox folder",
      "command": "mkdir -p sandbox; cd sandbox ;deno run -RW ../src/main.ts; cd .."
    }
  }
}